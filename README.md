# Сервис анализа отзывов для Газпромбанка

Решение кейса для хакатона MISIS LCT - сервис для анализа отзывов клиентов банка с мультилейбл классификацией и определением тональности.

## 🎯 Описание решения

Сервис выполняет:
1. **Мультилейбл классификацию** отзывов по банковским продуктам и услугам
2. **Анализ тональности** (положительно/нейтрально/отрицательно) для каждой темы отдельно
3. **REST API** для получения предсказаний в режиме inference

## 🏗️ Архитектура

### Технологический стек
- **Backend**: FastAPI, Python 3.11+
- **ML Pipeline**: LangChain, LangGraph
- **LLM**: Google Gemini (gemma-3-27b-it)
- **Deployment**: Docker, Uvicorn

### Подход к решению

#### 1. Кластеризация и категории
На основе анализа отзывов выделены следующие категории банковских продуктов/услуг:
- Дебетовые карты
- Кредитные карты
- Ипотека
- Автокредиты
- Кредиты
- Реструктуризация
- Вклады
- Переводы
- Дистанционное обслуживание
- Мобильное приложение
- Обслуживание
- Прочее

#### 2. Классификация
Используется LLM-based подход с двухэтапным агентом:
1. **Этап 1**: Классификация отзыва по категориям (мультилейбл)
2. **Этап 2**: Определение тональности для каждой категории отдельно

Агент реализован на основе LangGraph для гибкого управления pipeline.

#### 3. Батчинг
Для оптимизации API лимитов и ускорения обработки реализована батчинг-стратегия (по умолчанию 10 отзывов).

#### 4. Хранилище результатов
Все результаты классификации автоматически сохраняются в JSON-файл (`data/reviews.json`). Это позволяет:
- Строить статистику и аналитику
- Фильтровать отзывы по датам, категориям, источникам
- Отслеживать динамику тональности во времени
- Работать с историческими данными

## 📦 Установка и запуск

### Предварительные требования
- Python 3.11+
- Poetry
- Docker (опционально)
- Google API Key для Gemini

### Локальный запуск

1. **Клонируйте репозиторий**
```bash
git clone <repo-url>
cd misis-lct
```

2. **Установите зависимости**
```bash
poetry install
```

3. **Настройте переменные окружения**
```bash
cp .env.example .env
# Отредактируйте .env и укажите GOOGLE_API_KEY
```

4. **Запустите сервер**
```bash
poetry run uvicorn lct_gazprombank.main:app --host 0.0.0.0 --port 8000
```

Сервис будет доступен по адресу: http://localhost:8000

### Запуск через Docker

1. **Соберите образ**
```bash
docker build -t lct-gazprombank .
```

2. **Запустите контейнер**
```bash
docker run -p 8000:8000 -e GOOGLE_API_KEY=your_key lct-gazprombank
```

## 🔌 API Endpoints

### POST /predict
**Основной эндпоинт для проверки (согласно ТЗ раздел 2.4.1)**

Принимает только `id` и `text`. Результаты НЕ сохраняются в хранилище.

**Запрос:**
```json
{
    "data": [
        {
            "id": 1,
            "text": "Очень понравилось обслуживание в отделении, но мобильное приложение часто зависает."
        },
        {
            "id": 2,
            "text": "Кредитную карту одобрили быстро, но лимит слишком маленький."
        }
    ]
}
```

**Ответ:**
```json
{
    "predictions": [
        {
            "id": 1,
            "text": "Очень понравилось обслуживание...",
            "topics": ["Обслуживание", "Мобильное Приложение"],
            "sentiments": ["положительно", "отрицательно"],
            "date": null,
            "source": null
        },
        {
            "id": 2,
            "text": "Кредитную карту одобрили...",
            "topics": ["Кредитные Карты"],
            "sentiments": ["нейтрально"],
            "date": null,
            "source": null
        }
    ]
}
```

### POST /load-data
**Загрузка исторических данных для дашборда**

Принимает отзывы с обязательной датой и опциональным источником. Результаты сохраняются в хранилище.

**Запрос:**
```json
{
    "data": [
        {
            "id": 1,
            "text": "Отличный банк! Быстро одобрили ипотеку.",
            "date": "2024-03-15T10:30:00",
            "source": "banki.ru"
        },
        {
            "id": 2,
            "text": "Мобильное приложение постоянно виснет.",
            "date": "2024-03-16T14:20:00",
            "source": "sravni.ru"
        }
    ]
}
```

**Ответ:**
```json
{
    "loaded_count": 2,
    "predictions": [
        {
            "id": 1,
            "text": "Отличный банк! Быстро одобрили ипотеку.",
            "topics": ["Ипотека"],
            "sentiments": ["положительно"],
            "date": "2024-03-15T10:30:00",
            "source": "banki.ru"
        },
        {
            "id": 2,
            "text": "Мобильное приложение постоянно виснет.",
            "topics": ["Мобильное Приложение"],
            "sentiments": ["отрицательно"],
            "date": "2024-03-16T14:20:00",
            "source": "sravni.ru"
        }
    ]
}
```

### GET /categories
Получить список всех доступных категорий

**Ответ:**
```json
["Дебетовые карты", "Кредитные карты", "Ипотека", ...]
```

### GET /reviews
Получить отзывы с фильтрацией

**Параметры:**
- `start_date` - начальная дата (ISO формат)
- `end_date` - конечная дата
- `categories` - категории через запятую
- `sources` - источники через запятую
- `limit` - максимальное количество

**Пример:**
```
GET /reviews?start_date=2024-01-01T00:00:00&end_date=2024-12-31T23:59:59&categories=Ипотека,Кредитные карты&limit=100
```

### GET /statistics
Получить общую статистику по категориям

**Параметры:**
- `start_date` - начальная дата фильтра
- `end_date` - конечная дата фильтра
- `categories` - категории через запятую

**Ответ:**
```json
{
    "total_reviews": 150,
    "date_range": {"start": "2024-01-01T00:00:00", "end": "2024-12-31T23:59:59"},
    "categories": [
        {
            "category": "Кредитные карты",
            "total_count": 45,
            "positive_count": 20,
            "neutral_count": 15,
            "negative_count": 10,
            "positive_percent": 44.44,
            "neutral_percent": 33.33,
            "negative_percent": 22.22
        }
    ]
}
```

### GET /statistics/timeline
Получить временную динамику отзывов

**Параметры:**
- `start_date` - начальная дата
- `end_date` - конечная дата
- `categories` - категории через запятую
- `granularity` - гранулярность (day, week, month)

**Ответ:**
```json
{
    "categories": [
        {
            "category": "Кредитные карты",
            "timeline": [
                {
                    "date": "2024-01-01",
                    "positive": 5,
                    "neutral": 3,
                    "negative": 2,
                    "total": 10
                }
            ]
        }
    ]
}
```

### GET /health
Проверка работоспособности сервиса

### GET /
Информация об API

## 📊 Метрики качества

Согласно требованиям кейса, решение оценивается по:
- **F1-micro** для мультилейбл классификации (вес 70%)
- **Accuracy** для определения тональности (вес 30%)

## ⚙️ Конфигурация

Настройки определяются через переменные окружения:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `GOOGLE_API_KEY` | API ключ Google Gemini | *обязательно* |
| `LLM_NAME` | Модель для использования | `models/gemma-3-27b-it` |
| `PROJECT_NAME` | Название проекта | `Review Analysis API` |

## 🚀 Производительность

- Время обработки: ≤3 минуты для 250 отзывов
- Батчинг: по 10 отзывов для оптимизации API лимитов
- Retry-стратегия при превышении квот

## ⚠️ Важные замечания

1. **VPN**: Google Gemini API не работает на территории РФ. Требуется VPN.
2. **Лимиты API**: При превышении квот реализована автоматическая retry-стратегия с задержкой 60 сек.
3. **Нормализация**: Все категории и тональности автоматически нормализуются (lowercase, strip).

## 📝 Структура проекта

```
misis-lct/
├── data/
│   ├── categories.txt          # Список категорий
│   └── reviews.json            # Хранилище результатов (создается автоматически)
├── src/
│   └── lct_gazprombank/
│       ├── agent/              # LangGraph агент
│       │   ├── graph.py        # Граф агента
│       │   ├── prompts.py      # Промпты для LLM
│       │   ├── state.py        # Состояние агента
│       │   └── utils.py        # Утилиты и LLM wrapper
│       ├── core/               # Конфигурация
│       │   └── config.py       # Настройки приложения
│       ├── routers/            # API роуты
│       │   ├── predict.py      # Эндпоинт классификации
│       │   └── statistics.py   # Эндпоинты статистики
│       ├── schemas/            # Pydantic модели
│       │   └── review.py       # Схемы данных
│       ├── services/           # Бизнес-логика
│       │   ├── predict.py      # Сервис классификации
│       │   └── statistics.py   # Сервис статистики
│       ├── storage/            # Хранилище данных
│       │   └── storage.py      # Работа с JSON файлом
│       ├── utils/              # Общие утилиты
│       │   └── loader.py       # Загрузка категорий
│       └── main.py             # FastAPI приложение
├── Dockerfile
├── pyproject.toml
└── README.md
```

## 👥 Авторы

- **avolxn** - vlad.eldashov2303@mail.ru

## 📄 Лицензия

Проект разработан для участия в хакатоне MISIS LCT.

